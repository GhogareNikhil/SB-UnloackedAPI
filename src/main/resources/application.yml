server:
  port: 9090
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/sbRegistrionApp
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  
    
unlock-app:
   message:
     accUnlockedSuccess: Your account unlocked successfully
     invalidTempPwd: Invalid Temporary Password
    